# Windows D Drive Environment

- 项目固定在 `D:\AIProjects\manga-auto-colorizer`
- Python 固定为 `D:\CondaEnvs\manga-color-v2\python.exe`
- 所有缓存、模型、输出都放在 D 盘
- 所有脚本需要兼容 Windows PowerShell
- 所有路径处理使用 `pathlib`
