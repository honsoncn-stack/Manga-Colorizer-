# Library Storage

`library/` is the local reader-mode bookshelf for Manga Auto Colorizer.

Tracked files:

- `README_LIBRARY.md`

Ignored local user content:

- `library/books/`
- `library/library_index.json`

Imported manga, generated thumbnails, per-book color caches, and exported PDFs stay in this directory and are not committed to Git.
